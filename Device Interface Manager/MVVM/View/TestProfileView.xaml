<Window x:Class="Device_Interface_Manager.MVVM.View.TestProfileView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:viewmodel="clr-namespace:Device_Interface_Manager.MVVM.ViewModel"
        d:DataContext="{d:DesignInstance Type=viewmodel:TestProfileViewModel}"
        mc:Ignorable="d" 
        Background="Black"
        d:DesignHeight="450" d:DesignWidth="800">

    <Window.DataContext>
        <viewmodel:TestProfileViewModel/>
    </Window.DataContext>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <ComboBox 
                MinWidth="200"
                IsEditable="True"
                IsTextSearchEnabled="True"
                Text="{Binding SearchPMDGEventsText, UpdateSourceTrigger=PropertyChanged}"
                ItemsSource="{Binding PMDGEvents}"
                VirtualizingStackPanel.IsVirtualizing="True">
                <ComboBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel/>
                    </ItemsPanelTemplate>
                </ComboBox.ItemsPanel>
            </ComboBox>

            <ComboBox
                MinWidth="200"
                IsEditable="True"
                IsTextSearchEnabled="True"
                Text="{Binding SearchPMDGDataText}"
                ItemsSource="{Binding PMDGDataFieldNames}"
                VirtualizingStackPanel.IsVirtualizing="True">
                <ComboBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel/>
                    </ItemsPanelTemplate>
                </ComboBox.ItemsPanel>
            </ComboBox>
        </StackPanel>
    </Grid>
</Window>